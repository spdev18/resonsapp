<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#CCCCCC" /> <!-- Darker gray for pressed state -->
            <corners android:radius="24dp" /> <!-- Rounded button -->
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="#358DF3" /> <!-- Light gray background -->
            <corners android:radius="24dp" /> <!-- Rounded button -->
        </shape>
    </item>
</selector>
